# ========================
# 📄 .env.example — copia y renombra a:
#   - .env.development         (vite dev local)
#   - .env.production          (build detrás de proxy)
#   - .env.production.external (build sin proxy, dominios absolutos)
# ========================

# URL pública del sitio/panel
VITE_PUBLIC_SITE_URL=http://localhost:5173/

# Backend — elige relativo (proxy) o absoluto
# Relativo (detrás de Nginx reverse proxy):
# VITE_API_BASE=/api
# Absoluto (sin proxy):
# VITE_API_BASE=https://api.zajuna.edu.co/api

# Compat (si tu UI lee estas variantes)
VITE_API_BASE_URL=${VITE_API_BASE}
VITE_API_URL=${VITE_API_BASE}

# Rasa — relativo (proxy) o absoluto (sin proxy)
# Relativo:
# VITE_RASA_HTTP=/rasa
# VITE_RASA_WS=/ws
# Absoluto:
# VITE_RASA_HTTP=https://rasa.zajuna.edu.co
# VITE_RASA_WS=wss://rasa.zajuna.edu.co/ws
# VITE_RASA_WS_URL=${VITE_RASA_WS}

# Transporte del chat

VITE_CHAT_REST_URL=/api/chat
VITE_LOGIN_URL=https://zajuna.sena.edu.co/
VITE_ALLOWED_HOST_ORIGINS=https://zajuna.sena.edu.co,http://localhost:8080
VITE_AUTO_MINIMIZE_WIDGET=false

VITE_CHAT_TRANSPORT=rest
VITE_CHAT_REST_URL=/chat 
# Embeds/iframes permitidos
VITE_ALLOWED_HOST_ORIGINS=http://localhost:5173

# Login centralizado Zajuna
VITE_ZAJUNA_LOGIN_URL=http://localhost:5173/login

# Branding
VITE_ASSETS_BASE_URL=/
VITE_BOT_AVATAR=/embed/mi-avatar.png
VITE_BOT_LOADING=/embed/bot-loading.png
VITE_USER_AVATAR_DEFAULT=/user-avatar.png

# STT
ACTIONS_HTTP_TIMEOUT=10
ACTIONS_HTTP_RETRIES=2
# Si no tienes STT real todavía:
STT_PROVIDER=none

# GridFS
MONGO_URL=mongodb://mongo:27017
MONGO_DB=chatbot_admin
GRIDFS_ENABLED=true

VITE_ENABLE_CHAT_WIDGET=false
VITE_AUTO_MINIMIZE_WIDGET=false