# Strict-Transport-Security (solo tiene efecto cuando se sirve sobre HTTPS).
# Los navegadores ignoran este header si se entrega por HTTP plano.
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;