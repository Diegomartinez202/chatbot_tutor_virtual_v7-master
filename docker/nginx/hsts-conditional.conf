# Solo añade HSTS si el request llegó por HTTPS (útil detrás de proxy)
map $scheme $hsts_header {
  https "max-age=31536000; includeSubDomains; preload";
  default "";
}

add_header Strict-Transport-Security "$hsts_header" always;