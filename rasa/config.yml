# ============================================
# ⚙️ CONFIGURACIÓN RASA - Tutor Virtual (Rasa 3.1)
# ============================================
assistant_id: "tutorbot-local"
recipe: default.v1
version: "3.1"
language: "es"

# ============================================
# 🧩 PIPELINE NLP (Español + robusto)
# ============================================
pipeline:
  - name: SpacyNLP
    model: es_core_news_md

  # ÚNICO tokenizer: spaCy
  - name: SpacyTokenizer

  - name: RegexFeaturizer
    use_lookup_tables: true
    use_regexes: true
    use_word_boundaries: true

  - name: LexicalSyntacticFeaturizer

  - name: CountVectorsFeaturizer
    lowercase: true
    token_pattern: "(?u)\\b\\w+\\b"

  - name: CountVectorsFeaturizer
    analyzer: "char_wb"
    min_ngram: 1
    max_ngram: 4
    lowercase: true

  - name: SpacyFeaturizer

  - name: DIETClassifier
    epochs: 120
    constrain_similarities: true
    entity_recognition: true
    intent_classification: true

  - name: EntitySynonymMapper

  - name: ResponseSelector
    epochs: 80
    constrain_similarities: true

  # 👇 Aquí ya está bien el FallbackClassifier (esto va en pipeline, NO en policies)
  - name: FallbackClassifier
    threshold: 0.4
    ambiguity_threshold: 0.10

# ============================================
# 🧠 POLÍTICAS DE DECISIÓN (CORE)
# ============================================
policies:
  # 👇 Cambiamos para que el fallback core llame a action_handle_with_llm
  - name: RulePolicy
    core_fallback_threshold: 0.30
    core_fallback_action_name: "action_handle_with_llm"
    enable_fallback_prediction: true
    check_for_contradictions: true

  - name: MemoizationPolicy
    max_history: 5

  - name: TEDPolicy
    max_history: 10
    epochs: 120
    constrain_similarities: true
    batch_size: 64

# ============================================
# 🧩 Modo interactivo (opcional)
# ============================================
interactive:
  enable_entity_suggestions: true
  confirm_intent_threshold: 0.7
  auto_save_stories: true
  show_confidence_scores: true
