# ruta: rasa/data/rules/rules_enviar_soporte.yml
version: "3.1"

rules:

  - rule: Atajo rápido → Enviar soporte
    steps:
      - intent: enviar_soporte
      - action: action_enviar_soporte
      - action: utter_soporte_creado
